export enum AssetRestriction {
    Lock = 'Lock',
    UnLock = 'UnLock'
}

export enum PersmissionModel {
    View = 1,
    Share = 2,
    Update = 3,
    Exclusive = 4
  }


  export enum MetadataFileType {
    XML = 1,
    PDF = 2,
    CSV = 3
  }

