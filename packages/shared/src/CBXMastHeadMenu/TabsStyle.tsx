import { makeStyles } from "@material-ui/core";

export const MenuTabStyle = makeStyles({
    root : {
        padding : "0px",
        minWidth: "auto",
        textTransform: "none",
        minHeight: "auto",
        opacity: 1,
        overflowX: "visible",
    }
})
export const MenuTabsStyled = makeStyles({
    root : {
        overflowX: "visible",
    },

    scrollable : {
        overflowX : "visible"
    }
  
  })
