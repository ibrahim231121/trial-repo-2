{
  "name": "root",
  "private": true,
  "scripts": {
    "start": "(set PORT=3000 || export PORT=3000) && lerna run start --parallel",
    "build": "lerna run build"
  },
  "devDependencies": {
    "lerna": "^3.22.1"
  },
  "workspaces": {
    "packages": [
      "packages/*"
    ]
  }
}
