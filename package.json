{
  "name": "root",
  "private": true,
  "scripts": {
    "start": "lerna run start --parallel",
    "build": "lerna run build"
  },
  "devDependencies": {
    "lerna": "^3.22.1"
  },
  "workspaces": {
    "packages": [
      "packages/*"
    ]
  }
}
